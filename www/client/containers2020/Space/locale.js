const locale = {
  sectionHeaders: {
    space: {
      title: 'Space',
      body: 'Spacing helps us design experiences that scale with consistency, rhythm, and focus. We use increments of four to define all dimensions, padding, and margins for both block and inline elements.',
    },
  },
  tokenTables: {
    stack: {
      description: 'Stack spacing adds a margin to the bottom of elements. The stack space arranges components in a vertical rhythm.',
    },
    inline: {
      description: 'Inline spacing proportions create consistent right margins to elements. The inline space arranges components in a horizontal row.',
    },
    inset: {
      description: 'Inset spacing adds equal padding to all four sides of an element.',
    },
    exception: {
      description: 'There are always exceptions to the rule. For example, when the text or icon are vertically centered inside a component, the top and bottom padding can be any size.',
    },
  },
}

export default locale;
