import React from 'react';
import {
  DatePickerDefault,
} from './index';

export default class DatePickerKitchenSink extends React.Component {
  render() {
    return (
      <React.Fragment>
        <DatePickerDefault />
      </React.Fragment>
    );
  }
}
